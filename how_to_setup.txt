1. start docker
2. start cursor and venv setup and activate
3. start wsl ubuntu(wsl -d ubuntu)
4. docker-compose up -d
5. sudo systemctl start filebeat
6. sudo systemctl status filebeat
7. python -m stream.consumer
8. to send malicious logs in Ubuntu: for i in {1..6}; do ssh invaliduser@localhost; done
9. To flush all previous data: docker exec -it autoresponder-redis-1 redis-cli FLUSHALL

##############################################
# 🔄 AutoResponder Project Restart Sequence
# Author: You 💻 | For Local Dev & Demo Use
##############################################

# 1️⃣ Start Docker Desktop (Manually or Auto)
# 🐳 Required for Kafka, Zookeeper, Redis (if in containers)

# 2️⃣ Activate Python Virtual Environment
cd ~/AutoResponder
source .\venv\Scripts\Activate.ps1

# 3️⃣ Start WSL Ubuntu (if not already running)
Run in PowerShell or Command Prompt: > wsl -d Ubuntu

# 4️⃣ Start Kafka + Zookeeper + Redis (if using Docker Compose)
docker-compose up -d

# 5️⃣ Start Filebeat (inside WSL2 Ubuntu)
sudo systemctl start filebeat

# 6️⃣ Check Filebeat status
sudo systemctl status filebeat

# 7️⃣ Start the Kafka Consumer (Log Analyzer + Enricher + Telegram Alerts)
python -m stream.consumer

# 8️⃣ Simulate SSH Brute Force Logs (Run inside WSL2) 
for i in {1..6}; do ssh invaliduser@localhost; done